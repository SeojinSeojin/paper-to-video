# paper2video pipeline dependencies (Python 3.11+)

# HTTP + parsing
requests>=2.31
pyyaml>=6.0
pydantic>=2.6

# PDF ingest + figure extraction
pymupdf>=1.24

# Script generation (Gemini). Heavy/optional deps are lazy-imported so that
# --mock mode runs without them, but they are listed here for real runs.
google-genai>=1.0

# Text-to-speech + audio timeline
edge-tts>=6.1
pydub>=0.25

# Fallback figure cards
pillow>=10.2

# YouTube upload
google-api-python-client>=2.120
google-auth>=2.28
google-auth-oauthlib>=1.2
